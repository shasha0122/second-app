# PLAN.md — 청년 지원 정책 진단 앱

## 배경
자격이 있어도 몰라서 지원을 놓치는 청년을 위한 진단 앱. 정책을 나열만 하는 안내 페이지(`benefits.html`)에서 한 단계 나아가, 본인 상황을 입력하면 자격 여부를 판정해 준다.

## 사용자 (①)
- 자취를 시작한 사회초년생
- 취준생
- 무직 청년
- 제대 직후 청년
- 그 외 자신이 어떤 지원을 받을 수 있는지 모르는 모든 청년

이들은 "내가 이 정책 대상이 맞는지"를 스스로 계산하기 번거로워하거나(나이 계산에 병역 규정이 끼는 경우 등), 국토부 사업과 서울시 자체 사업처럼 조건이 비슷해 보이지만 실제로 다른 사업을 구분하지 못해 지원 기회를 놓친다.

## 데이터·규칙 원천
- 사실(수치·조건·확인일): `data/청년정책.md`
- 규칙(판정 원칙·예외): `AGENTS.md`
- 두 파일에 없는 정책·수치는 앱에 절대 추가하지 않는다.

## 기능 목록 (우선순위 순)

### P0 — 진단 입력 → 자격 판정 결과
- 입력 항목: 나이, 병역 여부/복무기간, 거주지(서울/비서울), 보증금, 월세, 소득(중위소득 %), 자산(일반재산·차량가액)
- 정책별(청년월세지원-국토부, 청년월세지원-서울시, 청년도약계좌)로 가능/불가를 단정적으로 표시
- 정책마다 병역 반영 방식이 다름 — 도약계좌는 복무기간만큼 그대로 차감, 서울시는 최대 3세 상한 연장, 국토부는 정확한 산정식이 출처마다 달라 미확정(단, 2년 복무자는 최소 36세까지 인정된다는 공통 사실만 사용)이므로 세 정책의 병역 로직을 절대 서로 섞어 쓰지 않는다
- `data/청년정책.md`에 없거나 미확정인 조건(국토부 소득·재산 기준, 도약계좌 지원 금액, 국토부 병역 연장 세부 구간)은 "원본 미기재" 또는 "미확정"으로 표시하고 그 부분만 단정하지 않음
- **완료 조건(DoD)**
  - `data/청년정책.md`의 정답 케이스 5개를 모두 입력했을 때 문서에 적힌 판정과 100% 일치
  - 국토부·서울시 월세지원을 별개로 각각 표시 (하나로 합쳐서 보여주지 않음)
  - 세 정책의 병역 규정이 각각 정책 고유의 방식대로만 적용됨 (다른 정책 방식을 빌려 쓰지 않음)
  - 원본에 없거나 미확정인 수치가 화면 어디에도 확정된 값처럼 나타나지 않음

### P0 — 예상 수령액 표시 (실비 지급액 계산)
- 월세지원(국토부·서울시) 판정이 "가능"일 때, 진단 결과에 예상 수령액을 함께 표시
- 계산식: `min(실제 월세, 20만원) × 지원기간(개월)`. 지원기간은 정책마다 다른 `data/청년정책.md` 값을 그대로 쓴다 — 국토부는 24개월(총 480만원 상한), 서울시는 12개월(총 240만원 상한, 생애 1회). 두 정책에 같은 개월수를 억지로 맞추지 않는다
- 도약계좌는 지원 금액이 원본 미기재이므로 예상 수령액을 계산·표시하지 않는다
- 가능/불가/확인 필요 판정 로직 자체는 건드리지 않는다 — 이 항목은 "가능"으로 이미 판정된 결과에 금액만 덧붙이는 표시 기능
- **완료 조건(DoD)**
  - 국토부 가능 케이스에서 `min(월세,20)×24`가 정확히 표시되고 480만원을 넘지 않음
  - 서울시 가능 케이스에서 `min(월세,20)×12`가 정확히 표시되고 240만원을 넘지 않음
  - 불가/확인 필요 판정에는 금액이 표시되지 않음
  - 도약계좌 카드에는 금액이 표시되지 않음 (원본 미기재 문구만 유지)
  - 정답 케이스 3, 4의 "가능" 결과에 표시되는 금액이 각 정책의 상한 계산과 일치

### P0 — 검수용 정답 케이스 노출
- `data/청년정책.md`의 "정답 케이스" 5개를 진단 과정/결과 화면에 예시로 노출 (사용자가 결과를 직접 검산 가능하도록)
- **완료 조건(DoD)**
  - 5개 케이스 전부가 페이지 본문에 보임 (링크나 별도 문서로 빼지 않음)
  - 각 케이스의 입력값과 판정 결과가 `data/청년정책.md`와 문구 단위까지 일치

### P1 — 확인일 표기 정리
- 현재 `benefits.html`은 칸마다 확인일이 반복돼 장식처럼 보인다는 지적(`수정사항.txt`)이 있었음
- 정책마다, 심지어 정책 내 항목마다 확인일이 다를 수 있으므로(예: 국토부는 기본 확인일과 병역 항목 확인일이 다름) 정책·항목 단위로 필요한 만큼만 표기하고 무의미한 반복은 없앤다
- **완료 조건(DoD)**
  - 같은 정책 블록 안에서 동일한 확인일이 장식적으로 반복 표기되지 않음
  - 표기된 확인일이 `data/청년정책.md`와 일치

### P2 — index.html 연동
- 포트폴리오 내 다른 페이지에서 진단 앱으로 접근 가능하게 링크 추가
- **완료 조건(DoD)**
  - `index.html`에서 진단 앱으로 가는 링크가 보임
  - 링크 클릭 시 실제로 진단 페이지로 이동

## 알려진 제약 / 아직 확정 못한 사실 (지어내지 않고 명시적으로 남겨둔 항목)
- 국토부 청년월세지원의 소득·재산 기준 (공식 사이트 접속 실패로 검증 못함)
- 국토부 청년월세지원의 병역 복무기간별 정확한 연령 상한 연장 폭 (2차 출처 간 수치 불일치)
- 청년도약계좌의 지원 금액
- 서울시 사업의 다음 회차 신청기간 (상시모집이 아니므로 매번 최신 공고 확인 필요)

이 항목들은 확인되기 전까지 앱에서 "원본 미기재" 또는 "미확정"으로만 표시하고, 임의의 숫자를 넣지 않는다.
